{% include('base.html.twig') %}

<div id="page-content-wrapper">
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <header class="card-header">
                        <h4 class="card-title mt-2">Ajouter une rubrique</h4>
                    </header>
                    <article class="card-body">
                        <form class="needs-validation" action="{{ url }}" method="POST" novalidate>
                            <div class="form-group">
                                <label for="validationCustom01">Rubrique</label>
                                <input class="form-control" type="text" id="validationCustom01" name="libelle" required>
                                {# vérif js #}
                                <div class="valid-feedback">
                                    Cela semble bon!
                                </div>
                                <div class="invalid-feedback">
                                    merci de renseigner une rubrique
                                </div>
                            </div>
                            <div class="form-group">
                                <input type="hidden" value="ajouterRubrique" name="action" />
                                <button type="submit" class="btn btn-danger btn-block">Ajouter </button>
                            </div>
                        </form>
                    </article>
                </div>
            </div>
        </div>
    </div>
</div>
